# ── Patient / medical data — NEVER commit (IRB No.018-0291) ───────────
# Surgical videos, extracted frames, and any per-case annotations.
*.mp4
*.avi
*.jpg
*.jpeg
*.png
*.tif
*.tiff
*.bmp
rawData/
Dataset/
Save_baseDiv/
ssLearning*/data*/
**/predictions_fold*.csv
**/okprobs*.parquet
**/okprobs*.csv
# Expert phase-time annotations (per-case clinical data). Current pipeline reads
# phase_times.xlsx; older code used supervised_phaseTime.xlsx. Ignore all xlsx to be safe.
*.xlsx
phase_times.xlsx
supervised_phaseTime.xlsx

# ── Trained models — "available on request" (paper Data Availability) ──
*.mat
*.pth
*.pt
*.onnx
*.h5
models/

# ── Results / caches / figures (regenerated locally) ──────────────────
results/
cache/
*.parquet

# ── Python ────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# ── OS / editor ───────────────────────────────────────────────────────
Thumbs.db
.DS_Store
*.asv
*.bak
